import Button from "../../../components/Button/Button";

const LoginWithGitHubButton = () => {
  return (
    <Button buttonType="solid" size="medium" onClick={() => {}}>
      Inicia sesión con GitHub
    </Button>
  );
};

export default LoginWithGitHubButton;
